syntax = "proto3";

option go_package = "userMicro/internal/api/grpcService";

enum ErrorCode {
  VALIDATION = 0;
  INTERNAL = 1;
}

message Error {
  ErrorCode code = 1;
  repeated FieldError detailedErrors = 2;

  message FieldError {
    string name = 1;
    string message = 2;
  }
}